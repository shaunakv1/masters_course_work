
<h1> Books By <%=h @publisher.name%></h1> 

	<ul>
		<% @books.each do |book| %>
		<li><%= link_to "#{book.title}", book %></li>
	  	<% end %>
	</ul>
	

<%= link_to 'Edit', edit_publisher_path(@publisher) %>|
<%= link_to 'delete', @publisher, :confirm => 'Are you sure?', :method => :delete %>|
<%= link_to 'Back', root_path %>

